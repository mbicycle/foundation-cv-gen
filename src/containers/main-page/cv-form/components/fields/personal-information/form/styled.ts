import { styled } from '@mui/material';

export const FormControlStyled = styled('form')(({ theme }) => ({
  margin: theme.spacing(5.5, 0),
}));

export const ContainerStyled = styled('div')(({ theme }) => ({
  margin: theme.spacing(5.5, 0),
}));
